sudo: required
language: generic 
services:
  - docker

before_install:
  - docker build -t alexpinsky/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true alexpinsky/docker-react npm run test